part of values;

class AppTextStyles {
  static final TextStyle splashHeader = GoogleFonts.lato(
      fontWeight: FontWeight.w600,
      fontSize: 35,
      shadows: [
        const Shadow(
            color: Colors.black, offset: Offset(0.0, 0.5), blurRadius: 0.5),
      ],
      color: Colors.teal);

  static final TextStyle titleHeader = GoogleFonts.lato(
      fontWeight: FontWeight.w600,
      fontSize: 25,
      shadows: [
        const Shadow(
            color: Colors.black, offset: Offset(0.0, 0.5), blurRadius: 0.5),
      ],
      color: Colors.black);

  static final TextStyle titleHeader2 = GoogleFonts.lato(
      fontWeight: FontWeight.w400,
      fontSize: 25,
      color: Colors.black);

  static final TextStyle header = GoogleFonts.lato(
      fontWeight: FontWeight.w600,
      fontSize: 22,
      shadows: [
        const Shadow(
            color: Colors.black, offset: Offset(0.0, 0.5), blurRadius: 0.5),
      ],
      color: Colors.black);

  static final TextStyle subHeader = GoogleFonts.lato(
      fontWeight: FontWeight.w500,
      height: 1.5,
      fontSize: 13,
      color: Colors.black.withOpacity(0.8));

  static final TextStyle skipButton = GoogleFonts.lato(
      fontWeight: FontWeight.w500,
      fontSize: 13,
      shadows: [
        const Shadow(
            color: Colors.black, offset: Offset(0.0, 0.2), blurRadius: 0.2),
      ],
      color: Colors.black);

  static final TextStyle onboardingButton = GoogleFonts.lato(
      fontWeight: FontWeight.w700,
      fontSize: 13,
      shadows: [
        const Shadow(
            color: Colors.black, offset: Offset(0.0, 0.2), blurRadius: 0.2),
      ],
      color: Colors.white);

  static final TextStyle authSelectText = GoogleFonts.lato(
      fontWeight: FontWeight.w600,
      fontSize: 15,
      shadows: [
        const Shadow(
            color: Colors.black, offset: Offset(0.0, 0.4), blurRadius: 0.4),
      ],
      color: Colors.black);

  static final TextStyle authUnselectText = GoogleFonts.lato(
      fontWeight: FontWeight.w500,
      fontSize: 14,
      shadows: [
        const Shadow(
            color: Colors.black, offset: Offset(0.0, 0.2), blurRadius: 0.2),
      ],
      color: Colors.grey);

  static final TextStyle normalText = GoogleFonts.lato(
      fontWeight: FontWeight.w500,
      fontSize: 12,
      shadows: [
        const Shadow(
            color: Colors.white, offset: Offset(0.0, 0.2), blurRadius: 0.2),
      ],
      color: Colors.black);

  static final TextStyle normalText2 = GoogleFonts.lato(
      fontWeight: FontWeight.w600,
      fontSize: 13,
      shadows: [
        const Shadow(
            color: Colors.black, offset: Offset(0.0, 0.2), blurRadius: 0.2),
      ],
      color: Colors.black);

  static final TextStyle snackBarText = GoogleFonts.lato(
      fontWeight: FontWeight.w500,
      fontSize: 12,
      shadows: [
        const Shadow(
            color: Colors.black, offset: Offset(0.0, 0.2), blurRadius: 0.2),
      ],
      color: Colors.white);

  static final TextStyle detailsText = GoogleFonts.lato(
      fontWeight: FontWeight.w500,
      fontSize: 12,
      shadows: [
        const Shadow(
            color: Colors.black, offset: Offset(0.0, 0.2), blurRadius: 0.2),
      ],
      color: Colors.black);

  static final TextStyle detailsText1 = GoogleFonts.lato(
      fontWeight: FontWeight.w600,
      fontSize: 12,
      shadows: [
        const Shadow(
            color: Colors.black, offset: Offset(0.0, 0.5), blurRadius: 0.5),
      ],
      color: Colors.green);

}
